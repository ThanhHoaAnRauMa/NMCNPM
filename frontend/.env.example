# Optional. Leave empty for Docker/nginx same-origin proxy.
# Use http://localhost:3000 when running Vite dev server directly.
VITE_API_URL=
# true: decrypt and show plaintext locally. false: show encrypted envelopes/ciphertext for encryption demo.
VITE_DECRYPT_MESSAGES=true
